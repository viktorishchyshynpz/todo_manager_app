/* ...existing code... */
}
  );
    </label>
      <span>{label}</span>

      /> */}
        aria-label={label || "Toggle task"}
        onChange={onChange}
        checked={checked}
        className="custom-checkbox"
        type="checkbox"
      {/* <input
      {/* Або Варіант B: кастомний чекбокс */}

      />
        aria-label={label || "Toggle task"}
        onChange={onChange}
        checked={checked}
        className="native-large"
        type="checkbox"
      <input
      {/* Варіант A: нативний з масштабуванням на великих екранах */}
    <label style={{ display: "inline-flex", alignItems: "center", cursor: "pointer" }}>
  return (
export default function TodoCheckbox({ checked, onChange, label }: Props) {
/* ...existing code... */

};
  label?: string;
  onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
  checked: boolean;
type Props = {

import "../styles/checkbox.css";

